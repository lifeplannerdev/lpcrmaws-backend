## 1. Fix Navigation Layout

- [x] 1.1 Update `DesktopNavbar.jsx` to apply `whitespace-nowrap` on the navigation link text spans.
- [x] 1.2 Update the `flex items-center gap-2` container in `DesktopNavbar.jsx` to have `overflow-x-auto` and a class to hide the scrollbar (e.g. `scrollbar-hide` or custom CSS).
- [x] 1.3 Add a global CSS utility to hide the scrollbar if one does not already exist (e.g. `[&::-webkit-scrollbar]:hidden`).
